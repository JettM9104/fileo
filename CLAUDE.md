# Fileo — Claude Instructions

At the start of every conversation, read `fileo.md` to get full context on the project.

After making any changes to the codebase, update `fileo.md` to reflect what changed — new endpoints, modified flows, fixed bugs, schema changes, new files, etc.
